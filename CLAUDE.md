# Project Workspace

This is the project repository. All agents must read and write files relative to this directory only.

- Do NOT access `/app/server/`, `/app/src/`, or any path outside this directory.
- Platform files live in `ix-forge/` — do not modify them.
- Do NOT run git commands.
